name: themer
description: "Flutter Theme manager package."
version: 0.0.3
homepage: 'https://magradze.dev'
repository: 'https://github.com/magradze/themer'

environment:
  sdk: '>=3.3.4 <4.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
